namespace CSharpOOP01
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region Part01

            #region Question01
            // a) The original copy stays the same beause it was copied by value

            // b) both variables get modified because it was copied by reference
            #endregion

            #region Question02
            // a) 1.all fields are public, so they can easily be modified from outside.
            // 2.Weight can be assignd zero or a negative number without validation.
            // 3.DeliveryFee can be assigned zero or a negative number without validation.

            // b) By enhancing encapsulation and only allowing valid and intended operations on the data
            #endregion

            #endregion
        }
    }
}
